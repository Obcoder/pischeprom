# Existing Laravel HTTPS URL, including the mobile API prefix.
# Copy to mobile/.env.local and set the actual server before building the APK.
VITE_MOBILE_API_URL=https://your-existing-laravel.example/api/mobile/v1
